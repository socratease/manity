# ============================================================================
# Manity Portfolio Backend - Environment Configuration
# ============================================================================
# Copy this file to .env and update with your actual values
# All settings are optional unless marked as required

# ============================================================================
# Database Configuration
# ============================================================================

# Database connection URL (default: sqlite:///./portfolio.db)
# Examples:
#   SQLite: sqlite:///./portfolio.db
#   PostgreSQL: postgresql://user:password@localhost/dbname
DATABASE_URL=sqlite:///./portfolio.db

# Default database paths for different environments (SQLite only)
# DEFAULT_DEV_DB_PATH=/home/c17420g/projects/manity-dev-data/portfolio.db
# DEFAULT_PROD_DB_PATH=/home/c17420g/projects/manity-data/portfolio.db

# ============================================================================
# OpenAI / LLM Configuration
# ============================================================================

# LLM provider: "openai" or "azure" (default: openai)
LLM_PROVIDER=openai

# Model name to use (default: gpt-5.1)
LLM_MODEL=gpt-5.1

# OpenAI API key (required for OpenAI provider)
OPENAI_API_KEY=your-openai-api-key-here

# OpenAI base URL for proxies or compatible APIs (default: https://api.openai.com/v1)
# OPENAI_BASE_URL=https://api.openai.com/v1

# ============================================================================
# Azure OpenAI Configuration (only needed if using Azure provider)
# ============================================================================

# Azure OpenAI endpoint URL
# AZURE_OPENAI_ENDPOINT=https://your-resource.openai.azure.com

# Azure OpenAI API key
# AZURE_OPENAI_API_KEY=your-azure-api-key-here

# Azure OpenAI deployment name
# AZURE_OPENAI_DEPLOYMENT=your-deployment-name

# Azure OpenAI API version (default: 2024-02-15-preview)
# AZURE_OPENAI_API_VERSION=2024-02-15-preview

# ============================================================================
# Admin & Authentication
# ============================================================================

# Admin authentication token for protected endpoints (required for admin operations)
# Can be set as either MANITY_ADMIN_TOKEN or ADMIN_KEY
MANITY_ADMIN_TOKEN=your-secure-admin-token-here
# ADMIN_KEY=your-secure-admin-token-here  # Alternative name

# ============================================================================
# Environment & Features
# ============================================================================

# Application environment: development, production, test, etc.
# Can be set as either MANITY_ENV or ENVIRONMENT
MANITY_ENV=development
# ENVIRONMENT=development  # Alternative name

# Enable demo data seeding on startup (default: false)
# Accepts: true/false, yes/no, 1/0, on/off (case-insensitive)
MANITY_ENABLE_DEMO_SEED=false

# ============================================================================
# CORS Configuration
# ============================================================================

# Comma-separated list of allowed frontend origins for CORS
# Example: http://localhost:3000,http://localhost:5173
# FRONTEND_ORIGINS=http://localhost:3000

# Regex pattern for allowed frontend origins (alternative to explicit list)
# Example: ^https?://(localhost|127\.0\.0\.1)(:\d+)?$
# FRONTEND_ORIGIN_REGEX=

# ============================================================================
# Notes
# ============================================================================
#
# - Environment variables take precedence over .env file values
# - Boolean values accept: true/false, yes/no, 1/0, on/off (case-insensitive)
# - URLs with trailing slashes will be automatically normalized
# - Environment names are normalized to lowercase
# - Empty or unset values will use the documented defaults
#
