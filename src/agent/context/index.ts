/**
 * Context Module Index
 *
 * Exports all context helpers.
 */

export {
  resolveProject,
  resolveTask,
  resolveSubtask,
  findPersonByName,
  cloneProjectDeep,
  generateActivityId,
  generateTaskId,
  generateSubtaskId,
  generateProjectId,
  syncProjectActivity,
  normalizeStakeholderEntry,
  normalizeStakeholderList,
  describeDueDate,
  buildProjectLookup,
  createHelpers,
} from './helpers';
